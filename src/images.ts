import preloader from 'spectacle/lib/utils/preloader'

export const images = {
    blockchainn: require('./assets/blockchainn.png'),
    blockchain: require('./assets/blockchain.png'),
    blocknchain: require('./assets/block&chain.png'),
    block: require('./assets/block.png')
}

preloader(images)
